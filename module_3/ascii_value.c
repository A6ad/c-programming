#include <stdio.h>

int main() {
    for(char i='A';i<='Z';i++){
        printf(" ascii of %c is : %d\n",i,i);
    }
printf("\n");

for(char i='a';i<='z';i++){
    printf("ascii of %c is: %d\n",i,i);
}
    return 0;
}