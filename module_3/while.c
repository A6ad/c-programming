#include <stdio.h>

int main() {
    int i=5; //initialized with a value
while(i>0){       //setting  a condition
    printf("%d\n",i);
    i--;           //decrementing the i until it is 0,when 0 the condition will be false and loop will termiante
}

    return 0;
}